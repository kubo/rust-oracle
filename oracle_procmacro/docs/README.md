This directory contains doc comments.

They are included from two locations.

1. Files in [`../src`](../src/) to generate documents.
2. [`../../src/procmacro.rs`](../../src/procmacro.rs) to run documentation tests.
